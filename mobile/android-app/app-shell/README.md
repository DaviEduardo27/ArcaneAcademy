# App Shell

Esta pasta sera a raiz do projeto Capacitor/Android.

Objetivo:

- receber `package.json` do shell mobile
- conter `capacitor.config`
- gerar a pasta `android/`
- abrir no Android Studio

Base web esperada:

- `../../webview-build`

Arquivo modelo incluido:

- `capacitor.config.template.json`

Observacao:

- nesta etapa nao foram instalados pacotes
- a estrutura foi preparada para que a inicializacao futura seja direta
